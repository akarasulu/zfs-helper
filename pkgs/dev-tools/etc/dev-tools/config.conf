# Dev-Tools Configuration File
# This file is managed by the package manager as a conffile

# Default editor for dev-tools
DEFAULT_EDITOR=vim

# Default project template
DEFAULT_TEMPLATE=basic

# Git configuration
GIT_AUTHOR_NAME="Developer"
GIT_AUTHOR_EMAIL="dev@localhost"

# Build settings
DEFAULT_BUILD_TOOL=make
PARALLEL_JOBS=4

# Logging
LOG_LEVEL=info
LOG_FILE=/var/log/dev-tools.log